function J_TerminalCost = J_TerminalCost_example(X0,Xf,t0,tf,settings)
%% J_TerminalCost関数の説明
%引数：
% X0(n_x×1, 初期時刻状態量ベクトル)
% Xf(n_x×1, 終端時刻状態量ベクトル)
% t0(スカラー値, 初期時刻)
% tf(スカラー値, 終端時刻)
% settings(設定値を格納する構造体)
%出力：
% J_TerminalCost(スカラー値, Terminal Cost値)

%% 関数定義
    J_TerminalCost = 0;
end
